---
layout: default
title: 一覧
permalink: /一覧/
---

# 一覧

{% assign all_indexes = site.pages 
  | where: "name", "index.md" 
  | where_exp: "p", "p.path contains 'all/'" 
  | sort: "path" %}

{% if all_indexes.size > 0 %}
  {% for p in all_indexes %}
    <hr>
    <h2>{{ p.path }}</h2>
    <div class="markdown-content">
      {% assign lines = p.content | split: "\n" %}
      {% assign filtered_lines = "" | split: "" %}

      {% assign inside_code = false %}
      {% for line in lines %}
        {% if line contains "```" %}
          {% assign inside_code = inside_code | xor: true %}
        {% elsif inside_code == false %}
          {% assign filtered_lines = filtered_lines | push: line %}
        {% endif %}
      {% endfor %}

      {{ filtered_lines | join: "\n" | markdownify }}
    </div>
  {% endfor %}
{% else %}
  <p>all フォルダ内に index.md は見つかりませんでした。</p>
{% endif %}
