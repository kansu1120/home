---
layout: default
title: 全てのページ一覧
permalink: /all-pages/
---

# 一覧

<ul>
{% assign md_pages = site.pages
  | where_exp: "p", "p.name != 'index.md'"   # 自分自身を除外
  | sort: "path" %}

{% for p in md_pages %}
  <li>
    <!-- HTML ページとしてリンク -->
    <a href="{{ p.url }}">
      {{ p.title | default: p.name | replace: ".md", "" }}
    </a>
    <small>({{ p.path }})</small> <!-- 階層確認用 -->
  </li>
{% endfor %}
</ul>
