---
layout: default
title: All フォルダ内 index.md 全文表示
permalink: /all-full/
---

# All フォルダ内 index.md 全文表示（先頭4行非表示）

{% assign all_indexes = site.pages 
  | where: "name", "index.md" 
  | where_exp: "p", "p.path contains 'all/'" 
  | sort: "path" %}

{% if all_indexes.size > 0 %}
  {% for p in all_indexes %}
    <hr>
    <h2>{{ p.path }}</h2>
    <div class="markdown-content">
      {% assign lines = p.content | split: "\n" %}
      {% if lines.size > 4 %}
        {% assign display_lines = lines | slice: 4, lines.size %}
        {{ display_lines | join: "\n" }}
      {% else %}
        <!-- 行数が4以下なら何も表示しない -->
        <p><em>表示できる内容がありません</em></p>
      {% endif %}
    </div>
  {% endfor %}
{% else %}
  <p>all フォルダ内に index.md は見つかりませんでした。</p>
{% endif %}
